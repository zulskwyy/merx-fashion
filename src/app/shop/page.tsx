"use client";
import React,{useEffect,useMemo,useState} from "react"; import ProductCard from "@/components/common/ProductCard"; import {products} from "@/data/products"; import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "@/components/ui/select"; import {Button} from "@/components/ui/button"; import Link from "next/link"; import {useI18n} from "@/lib/i18n";
const PAGE_SIZE=12;
export default function ShopPage(){const {t}=useI18n();const [query,setQuery]=useState("");const [category,setCategory]=useState("All");const [color,setColor]=useState("All");const [gender,setGender]=useState("All");const [sort,setSort]=useState("popular");const [page,setPage]=useState(1);const [open,setOpen]=useState(false);useEffect(()=>{const sp=new URLSearchParams(window.location.search);setQuery(sp.get("q")||"");setCategory(sp.get("category")||"All");setGender(sp.get("gender")||"All");const requested=sp.get("sort");if(requested)setSort(["new","low","high","popular"].includes(requested)?requested:"popular")},[]);const categories=["All",...Array.from(new Set(products.map(p=>p.category)))];const colors=["All",...Array.from(new Set(products.map(p=>p.color)))];const genders=["All",...Array.from(new Set(products.map(p=>p.gender)))];const filtered=useMemo(()=>{let out=products.filter(p=>{const hay=`${p.title} ${p.category} ${p.color} ${p.gender}`.toLowerCase();return(!query||hay.includes(query.toLowerCase()))&&(category==="All"||p.category===category)&&(color==="All"||p.color===color)&&(gender==="All"||p.gender===gender)});if(sort==="low")out.sort((a,b)=>a.price-b.price);if(sort==="high")out.sort((a,b)=>b.price-a.price);if(sort==="new")out.sort((a,b)=>a.id-b.id);if(sort==="popular")out.sort((a,b)=>b.rating-a.rating);return out},[query,category,color,gender,sort]);const totalPages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));const shown=filtered.slice((page-1)*PAGE_SIZE,page*PAGE_SIZE);const reset=()=>{setQuery("");setCategory("All");setColor("All");setGender("All");setPage(1)};const FilterPanel=()=> <div className="space-y-5"><div><b className="text-lg">{t("filters")}</b><label className="block text-sm mt-4 mb-2">{t("search")}</label><input value={query} onChange={e=>{setQuery(e.target.value);setPage(1)}} placeholder={`${t("search")} produk...`} className="w-full rounded-xl bg-[#F3EFE7] px-4 py-3 outline-none"/></div>{[[t("category"),categories,category,setCategory],[t("color"),colors,color,setColor],[t("gender"),genders,gender,setGender]].map(([label,items,value,setter]:any)=><div key={label as string}><label className="block text-sm mb-2">{label as string}</label><select value={value as string} onChange={e=>{setter(e.target.value);setPage(1)}} className="w-full rounded-xl border border-black/10 px-4 py-3 bg-white">{(items as string[]).map(v=><option key={v}>{v}</option>)}</select></div>)}<Button type="button" onClick={reset} variant="outline" className="w-full">{t("reset")}</Button></div>;
return <main className="pb-20"><div className="max-w-frame mx-auto px-4 xl:px-0"><div className="flex items-center gap-2 text-sm text-black/50 py-5"><Link href="/">{t("home")}</Link><span>/</span><span>{t("shop")}</span></div><div className="flex items-center justify-between mb-7"><div><h1 className="font-bold text-3xl md:text-[40px]">{t("shop")} {t("viewAll").toLowerCase()}</h1><p className="text-black/60 mt-1">{products.length} produk dari koleksi foto yang kamu berikan.</p></div><Button className="md:hidden bg-[#1B2A4A]" onClick={()=>setOpen(true)}>{t("filters")}</Button></div><div className="flex md:space-x-7 items-start"><aside className="hidden md:block min-w-[270px] max-w-[270px] border border-black/10 rounded-2xl p-5"><FilterPanel/></aside><section className="flex flex-col w-full gap-6"><div className="flex flex-wrap items-center justify-between gap-3"><span className="text-sm text-black/60">{t("showing")} {filtered.length?(page-1)*PAGE_SIZE+1:0}-{Math.min(page*PAGE_SIZE,filtered.length)} dari {filtered.length}</span><div className="flex items-center gap-2"><span>{t("sortBy")}</span><Select value={sort} onValueChange={v=>{setSort(v);setPage(1)}}><SelectTrigger className="w-[180px] border-none bg-[#F3EFE7] rounded-full"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="popular">{t("popular")}</SelectItem><SelectItem value="new">{t("newest")}</SelectItem><SelectItem value="low">{t("lowHigh")}</SelectItem><SelectItem value="high">{t("highLow")}</SelectItem></SelectContent></Select></div></div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 lg:gap-6">{shown.map(p=><ProductCard key={p.id} data={p}/>)}{!shown.length&&<div className="col-span-full py-20 text-center text-black/50">{t("noProducts")}</div>}</div><div className="flex items-center justify-center gap-2 pt-4">{Array.from({length:totalPages},(_,i)=>i+1).map(n=><Button key={n} variant={n===page?"default":"outline"} className={n===page?"bg-[#1B2A4A]":""} onClick={()=>setPage(n)}>{n}</Button>)}</div></section></div></div>{open&&<div className="fixed inset-0 z-50 md:hidden"><div className="absolute inset-0 bg-black/40" onClick={()=>setOpen(false)}/><div className="absolute right-0 top-0 h-full w-[88%] max-w-sm bg-white p-5 overflow-y-auto"><button onClick={()=>setOpen(false)} className="absolute right-4 top-4 text-2xl" aria-label="Tutup">×</button><div className="pt-10"><FilterPanel/></div></div></div>}</main>}
