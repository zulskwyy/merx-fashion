import { NextResponse } from "next/server";
import { getAdminEmail } from "@/lib/server/admin-auth";
import { dbConfigured, supabaseRequest } from "@/lib/server/supabase";
export async function GET(){ if(!getAdminEmail())return NextResponse.json({error:"Unauthorized"},{status:401}); if(!dbConfigured)return NextResponse.json({error:"Database belum dikonfigurasi."},{status:503}); const rows=await supabaseRequest<any[]>("store_settings?select=*&id=eq.1&limit=1"); return NextResponse.json(rows?.[0]||null); }
export async function PATCH(req:Request){ if(!getAdminEmail())return NextResponse.json({error:"Unauthorized"},{status:401}); if(!dbConfigured)return NextResponse.json({error:"Database belum dikonfigurasi."},{status:503}); const b=await req.json(); const row={store_name:b.storeName||"MERX",primary_color:b.primaryColor||"#1B2A4A",accent_color:b.accentColor||"#F3EFE7",hero_title:b.heroTitle||"",hero_description:b.heroDescription||"",hero_image_url:b.heroImageUrl||"",updated_at:new Date().toISOString()}; const data=await supabaseRequest("store_settings?id=eq.1",{method:"PATCH",headers:{Prefer:"return=representation"},body:JSON.stringify(row)}); return NextResponse.json(data); }
