import { NextResponse } from "next/server";
function esc(s:string){return s.replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]||c));}
export async function POST(req:Request){try{const body=await req.json();const {customer,items,total,payment}=body||{};if(!customer?.name||!/^\S+@\S+\.\S+$/.test(customer?.email||"")||!Array.isArray(items)||!items.length||typeof total!=="number")return NextResponse.json({error:"Invalid checkout data"},{status:400});const orderId=`MERX-${Date.now().toString(36).toUpperCase()}`;let emailSent=false;const apiKey=process.env.RESEND_API_KEY;const from=process.env.RESEND_FROM_EMAIL;if(apiKey&&from){const html=`<div style="font-family:Arial,sans-serif;max-width:640px;margin:auto"><h1>MERX payment receipt</h1><p>Hi ${esc(customer.name)}, your test-mode payment is confirmed.</p><p><b>Order:</b> ${orderId}</p><p><b>Payment:</b> ${esc(payment||'test')}</p><p><b>Total:</b> Rp ${Number(total).toLocaleString('id-ID')}</p><h3>Items</h3><ul>${items.map((i:any)=>`<li>${esc(i.name)} × ${Number(i.quantity)||1} (${esc((i.attributes||[]).join(' / '))})</li>`).join('')}</ul><p>This receipt was generated by your MERX checkout flow.</p></div>`;const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[customer.email],subject:`MERX receipt ${orderId}`,html})});emailSent=r.ok;}return NextResponse.json({orderId,emailSent},{status:200});}catch{return NextResponse.json({error:"Unable to process checkout"},{status:500})}}
