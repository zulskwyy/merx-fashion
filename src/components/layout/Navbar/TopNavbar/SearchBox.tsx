"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import Image from "next/image"; import { useI18n } from "@/lib/i18n";
export default function SearchBox(){const router=useRouter();const {t}=useI18n();const [q,setQ]=useState("");const submit=(e:React.FormEvent)=>{e.preventDefault();router.push(`/shop?q=${encodeURIComponent(q)}`)};return <form onSubmit={submit} className="hidden lg:flex items-center gap-2 rounded-full bg-[#F3EFE7] px-4 py-2 w-full max-w-[360px]"><Image src="/icons/search-black.svg" width={18} height={18} alt=""/><input value={q} onChange={e=>setQ(e.target.value)} type="search" placeholder={`${t("search")} produk...`} className="bg-transparent placeholder:text-black/40 outline-none w-full"/></form>}
