"use client";
import React,{useEffect,useState} from "react";import {useRouter} from "next/navigation";import Image from "next/image";import InputGroup from "@/components/ui/input-group";
export default function SearchBox(){const router=useRouter();const [q,setQ]=useState("");useEffect(()=>{setQ(new URLSearchParams(window.location.search).get("q")||"")},[]);const go=(e?:React.FormEvent)=>{e?.preventDefault();router.push(q.trim()?`/shop?q=${encodeURIComponent(q.trim())}`:"/shop")};return <form onSubmit={go} className="hidden md:flex mr-3 lg:mr-10"><InputGroup className="bg-[#F3EFE7]"><InputGroup.Text><Image src="/icons/search.svg" height={20} width={20} alt="search"/></InputGroup.Text><InputGroup.Input value={q} onChange={e=>setQ(e.target.value)} type="search" placeholder="Search for products..." className="bg-transparent placeholder:text-black/40"/></InputGroup></form>}
