import React from "react";
import {Sheet,SheetClose,SheetContent,SheetHeader,SheetTitle,SheetTrigger} from "@/components/ui/sheet";
import Image from "next/image"; import Link from "next/link"; import {cn} from "@/lib/utils"; import {integralCF} from "@/styles/fonts"; import {NavMenu} from "../navbar.types";
export default function ResTopNavbar({data}:{data:NavMenu}){return <Sheet><SheetTrigger asChild className="cursor-pointer"><Image src="/icons/menu.svg" height={22} width={22} alt="Menu"/></SheetTrigger><SheetContent side="left" className="overflow-y-auto"><SheetHeader className="mb-10"><SheetTitle asChild><SheetClose asChild><Link href="/" className={cn(integralCF.className,"text-2xl text-[#1B2A4A]")}>MERX</Link></SheetClose></SheetTitle></SheetHeader><div className="flex flex-col items-start">{data.map(item=>item.type==="MenuItem"?<SheetClose asChild key={item.id}><Link href={item.url||"/"} className="mb-5">{item.label}</Link></SheetClose>:<div key={item.id} className="mb-5 w-full"><div className="font-medium mb-3">{item.label}</div><div className="flex flex-col border-l pl-4">{item.children.map(child=><SheetClose asChild key={child.id}><Link href={child.url||"/"} className="py-2">{child.label}</Link></SheetClose>)}</div></div>)}</div></SheetContent></Sheet>}
