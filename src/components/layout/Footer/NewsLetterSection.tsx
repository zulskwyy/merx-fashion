"use client";
import {Button} from "@/components/ui/button"; import InputGroup from "@/components/ui/input-group"; import {cn} from "@/lib/utils"; import {integralCF} from "@/styles/fonts"; import React,{useState} from "react"; import {useI18n} from "@/lib/i18n";
export default function NewsLetterSection(){const {t}=useI18n();const [email,setEmail]=useState("");const [done,setDone]=useState(false);const submit=()=>{if(!email.includes("@"))return;setDone(true)};return <div className="relative grid grid-cols-1 md:grid-cols-2 py-9 md:py-11 px-6 md:px-16 max-w-frame mx-auto bg-[#1B2A4A]"><p className={cn(integralCF.className,"font-bold text-[30px] md:text-[38px] text-white mb-8 md:mb-0")}>{t("latestOffers")}</p><div className="flex items-center"><div className="flex flex-col w-full max-w-[360px] mx-auto"><InputGroup className="flex bg-white mb-3"><InputGroup.Input value={email} onChange={e=>{setEmail(e.target.value);setDone(false)}} type="email" name="email" placeholder={t("emailPlaceholder")} className="bg-transparent placeholder:text-black/40"/></InputGroup><Button variant="secondary" onClick={submit} className="text-sm h-12 bg-white" type="button">{done?t("subscribed"):t("subscribe")}</Button></div></div></div>}
