"use client";import React from "react";import {cn} from "@/lib/utils";import {useI18n} from "@/lib/i18n";export default function SizeSelection({value,onChange,sizes}:{value:string;onChange:(v:string)=>void;sizes:string[]}){const {t}=useI18n();return <div className="flex flex-col"><span className="text-sm sm:text-base text-black/60 mb-4">{t("size")}</span><div className="flex items-center flex-wrap gap-2">{sizes.map(size=><button key={size} type="button" className={cn("border border-black/15 bg-transparent px-5 lg:px-6 py-2.5 lg:py-3 text-sm lg:text-base transition-colors",value===size&&"bg-[#1B2A4A] border-[#1B2A4A] font-medium text-white")} onClick={()=>onChange(size)}>{size}</button>)}</div></div>}
