"use client";
import React,{useState} from "react";
import {Product} from "@/types/product.types";
export default function PhotoSection({data}:{data:Product}){const [selected,setSelected]=useState(data.gallery[0]||data.srcUrl);const gallery=Array.from(new Set(data.gallery));return <div className="flex flex-col-reverse lg:flex-row lg:space-x-3.5"><div className="flex lg:flex-col space-x-3 lg:space-x-0 lg:space-y-3.5 w-full lg:w-fit items-center lg:justify-start justify-center py-2 lg:py-0">{gallery.map((photo,index)=><button key={photo+index} type="button" className={"bg-[#F3EFE7] rounded-[13px] w-20 h-20 xl:w-[100px] xl:h-[110px] overflow-hidden "+(selected===photo?"ring-2 ring-[#1B2A4A]":"")} onClick={()=>setSelected(photo)}><img src={photo} width={220} height={220} className="w-full h-full object-cover" alt={`${data.title} view ${index+1}`}/></button>)}</div><div className="flex items-center justify-center bg-[#F3EFE7] rounded-[13px] sm:rounded-[20px] w-full mx-auto h-full max-h-[560px] min-h-[330px] overflow-hidden mb-3 lg:mb-0"><img src={selected} width={700} height={700} className="w-full h-full object-cover" alt={data.title}/></div></div>}
