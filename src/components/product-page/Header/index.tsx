"use client";
import React,{useState} from "react"; import PhotoSection from "./PhotoSection"; import {Product} from "@/types/product.types"; import {integralCF} from "@/styles/fonts"; import Rating from "@/components/ui/Rating"; import ColorSelection from "./ColorSelection"; import SizeSelection from "./SizeSelection"; import AddToCardSection from "./AddToCardSection"; import {discountedPrice,formatIDR} from "@/lib/catalog"; import {useI18n} from "@/lib/i18n";
export default function Header({data}:{data:Product}){const {t}=useI18n();const [color,setColor]=useState(data.color);const [size,setSize]=useState(data.sizes[Math.min(2,data.sizes.length-1)]);const sale=discountedPrice(data);return <div className="grid grid-cols-1 md:grid-cols-2 gap-5"><PhotoSection data={data}/><div><h1 className={`${integralCF.className} text-2xl md:text-[40px] md:leading-[40px] mb-3`}>{data.title}</h1><div className="flex items-center mb-3"><Rating initialValue={data.rating} allowFraction SVGclassName="inline-block" emptyClassName="fill-gray-50" size={25} readonly/><span className="text-xs sm:text-sm ml-3">{data.rating.toFixed(1)}/5 <span className="text-black/40">({data.reviewCount})</span></span></div><div className="flex items-center gap-3 mb-5"><span className="font-bold text-2xl sm:text-[32px]">{formatIDR(sale)}</span>{sale<data.price&&<><span className="font-bold text-black/40 line-through text-2xl sm:text-[32px]">{formatIDR(data.price)}</span><span className="font-medium text-xs py-1.5 px-3 rounded-full bg-[#7A1F2B]/10 text-[#7A1F2B]">-{data.discount.percentage}%</span></>}</div><p className="text-sm sm:text-base text-black/60 mb-5">{data.description}</p><hr className="border-t-black/10 mb-5"/><div className="text-sm mb-2">{t("selectedColor")}</div><ColorSelection value={color} onChange={setColor} colors={[data.color]}/><hr className="border-t-black/10 my-5"/><div className="text-sm mb-2">{t("size")}</div><SizeSelection value={size} onChange={setSize} sizes={data.sizes}/><hr className="hidden md:block border-t-black/10 my-5"/><AddToCardSection data={data} size={size} color={color}/></div></div>}
