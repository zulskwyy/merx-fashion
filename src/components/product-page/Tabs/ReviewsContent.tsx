"use client";import React,{useMemo,useState} from "react";import {Button} from "@/components/ui/button";import ReviewCard from "@/components/common/ReviewCard";import {Product} from "@/types/product.types";import {useI18n} from "@/lib/i18n";export default function ReviewsContent({product}:{product:Product}){const {t}=useI18n();const [sort,setSort]=useState("latest");const [count,setCount]=useState(4);const reviews=useMemo(()=>{const data=[...product.reviews];if(sort==="highest")data.sort((a,b)=>b.rating-a.rating);if(sort==="oldest")data.sort((a,b)=>a.date.localeCompare(b.date));if(sort==="latest")data.sort((a,b)=>b.date.localeCompare(a.date));return data},[product.reviews,sort]);return <section><div className="flex items-center justify-between flex-col sm:flex-row mb-5"><div><h3 className="text-xl sm:text-2xl font-bold">{t("reviews")}</h3><span className="text-sm text-black/50">{product.reviewCount} {t("reviews").toLowerCase()}</span></div><div className="flex gap-2 mt-4 sm:mt-0"><select value={sort} onChange={e=>setSort(e.target.value)} className="h-12 rounded-full bg-[#F3EFE7] px-4 text-sm"><option value="latest">Terbaru</option><option value="highest">Rating tertinggi</option><option value="oldest">Terlama</option></select><Button type="button" className="bg-[#1B2A4A]" onClick={()=>window.alert(t("writeReview"))}>{t("writeReview")}</Button></div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-5 mb-6">{reviews.slice(0,count).map(r=><ReviewCard key={r.id} data={r} isAction isDate/>)}</div>{count<reviews.length&&<Button variant="outline" className="w-full sm:w-auto" onClick={()=>setCount(v=>v+4)}>{t("loadMore")}</Button>}</section>}
