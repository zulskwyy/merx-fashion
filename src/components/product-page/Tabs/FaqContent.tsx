"use client";import React from "react";import {Accordion,AccordionContent,AccordionItem,AccordionTrigger} from "@/components/ui/accordion";import {Product} from "@/types/product.types";import {useI18n} from "@/lib/i18n";export default function FaqContent({product}:{product:Product}){const {t}=useI18n();return <section><h3 className="text-xl sm:text-2xl font-bold mb-5">{t("faqs")}</h3><Accordion type="single" collapsible>{product.faqs.map((faq,idx)=><AccordionItem key={idx} value={`item-${idx+1}`}><AccordionTrigger className="text-left">{faq.question}</AccordionTrigger><AccordionContent>{faq.answer}</AccordionContent></AccordionItem>)}</Accordion></section>}
