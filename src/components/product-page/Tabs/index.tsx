"use client";
import {Button} from "@/components/ui/button";import {cn} from "@/lib/utils";import React,{useState} from "react";import ProductDetailsContent from "./ProductDetailsContent";import ReviewsContent from "./ReviewsContent";import FaqContent from "./FaqContent";import {Product} from "@/types/product.types";
export default function Tabs({product}:{product:Product}){const [active,setActive]=useState(1);return <div><div className="flex items-center mb-6 sm:mb-8 overflow-x-auto">{[[1,"Product Details"],[2,"Rating & Reviews"],[3,"FAQs"]].map(([id,label])=><Button key={id} variant="ghost" type="button" className={cn(active===id?"border-black border-b-2 font-medium":"border-b border-black/10 text-black/60 font-normal","p-5 sm:p-6 rounded-none flex-1 min-w-[150px]")} onClick={()=>setActive(id as number)}>{label}</Button>)}</div><div className="mb-12 sm:mb-16">{active===1&&<ProductDetailsContent product={product}/>} {active===2&&<ReviewsContent product={product}/>} {active===3&&<FaqContent product={product}/>}</div></div>}
